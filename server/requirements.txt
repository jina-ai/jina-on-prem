fastapi>=0.115.0
uvicorn[standard]>=0.32.0
transformers>=4.48.0,<5.0.0
torch>=2.5.0
sentence-transformers>=3.3.0
einops>=0.8.0
pillow>=10.0.0
numpy>=1.24.0
huggingface_hub>=0.27.0
accelerate>=1.2.0
timm>=1.0.0
peft>=0.10.0

# Optional multimodal dependencies (omni models only):
# librosa soundfile    # audio decoding (.wav, .mp3, .flac, .ogg)
# av                   # video decoding (.mp4, .avi, .mov, .mkv, .webm)
# pdf2image pypdfium2  # PDF rendering
